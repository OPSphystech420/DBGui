### [Physical model](/models/physical_model.md)
—   structure of tables, fields, data types, and constraints.

### Contents

- `client_logins` – storage of user login records;

- `database_connections` – manages database connection settings;

- `error_logs` – logs errors;

- `roles`, `user_actions`, `user_devices`, `user_reviews`, `user_roles`, `user_settings` and `users` – stores and manages users, their actions, devices, reviews, and roles.

